"use client";

import React from 'react'
import Link from 'next/link'
import { motion } from 'framer-motion'

const FadeIn = ({ children, delay = 0 }: { children: React.ReactNode; delay?: number }) => (
  <motion.div
    initial={{ opacity: 0, y: 12 }}
    whileInView={{ opacity: 1, y: 0 }}
    viewport={{ once: true }}
    transition={{ duration: 0.6, delay, ease: [0.16, 1, 0.3, 1] }}
  >
    {children}
  </motion.div>
)

export default function HomePage() {
  return (
    <div className="min-h-screen bg-background text-foreground selection:bg-accent-blue selection:text-accent-blue-text">
      {/* Hero Section - Asymmetric Split */}
      <section className="py-24 md:py-32 px-6">
        <div className="max-w-5xl mx-auto grid grid-cols-12 gap-8 items-center">
          <div className="col-span-12 lg:col-span-7 text-left">
            <FadeIn>
              <span className="block text-xs uppercase tracking-widest text-muted mb-6 font-medium">
                Professional Identity Architecture
              </span>
              <h1 className="text-6xl md:text-8xl leading-[1.1] tracking-tighter mb-8">
                Architecture <br />
                <span className="italic">for leadership.</span>
              </h1>
              <p className="text-lg md:text-xl text-muted max-w-xl mb-12 leading-relaxed">
                A focused system for high-stakes professional communication.
                Return to the essence of clarity and typographic rigor.
              </p>
              <div className="flex flex-col sm:flex-row gap-4 justify-start">
                <Link
                  href="/register"
                  className="bg-foreground text-background rounded-mono px-8 py-4 transition-transform duration-200 active:scale-95 font-medium text-center"
                >
                  Start Building
                </Link>
                <Link
                  href="/register"
                  className="bg-transparent border border-border text-foreground rounded-mono px-8 py-4 transition-colors duration-200 hover:bg-surface font-medium text-center"
                >
                  Explore Systems
                </Link>
              </div>
            </FadeIn>
          </div>
          <div className="hidden lg:block lg:col-span-5">
            <FadeIn delay={0.2}>
              <div className="relative aspect-[4/5] overflow-hidden rounded-mono border border-border">
                <img
                  src="https://picsum.photos/seed/executive-studio/800/1000?grayscale"
                  alt="Minimalist professional studio"
                  className="w-full h-full object-cover opacity-80"
                />
                <div className="absolute inset-0 bg-gradient-to-t from-background/20 to-transparent" />
              </div>
            </FadeIn>
          </div>
        </div>
      </section>

      {/* Features Section - Bento Grid */}
      <section className="py-32 px-6 bg-surface">
        <div className="max-w-5xl mx-auto">
          <FadeIn>
            <div className="text-center mb-20">
              <span className="block text-xs uppercase tracking-widest text-muted mb-4 font-medium">
                Core Principles
              </span>
              <h2 className="text-4xl md:text-6xl tracking-tighter">
                Logic over ornament.
              </h2>
            </div>
          </FadeIn>

          <div className="grid grid-cols-12 gap-4">
            <FadeIn delay={0.1}>
              <div className="col-span-12 md:col-span-8 p-10 border border-border bg-background rounded-mono transition-shadow duration-200 hover:shadow-sm">
                <h3 className="text-2xl mb-4">Typographic Rigor</h3>
                <p className="text-muted leading-relaxed max-w-lg">
                  We employ a strict hierarchy of geometric sans and editorial serifs.
                  Readability is treated as a technical requirement, not a stylistic choice.
                </p>
              </div>
            </FadeIn>
            <FadeIn delay={0.2}>
              <div className="col-span-12 md:col-span-4 p-10 border border-border bg-background rounded-mono transition-shadow duration-200 hover:shadow-sm">
                <h3 className="text-2xl mb-4">ATS Sync</h3>
                <p className="text-muted leading-relaxed">
                  Structured data optimized for algorithmic parsing and human review.
                </p>
              </div>
            </FadeIn>
            <FadeIn delay={0.3}>
              <div className="col-span-12 md:col-span-4 p-10 border border-border bg-background rounded-mono transition-shadow duration-200 hover:shadow-sm">
                <h3 className="text-2xl mb-4">Precision PDF</h3>
                <p className="text-muted leading-relaxed">
                  Zero-shift export. What you see in the editor is exactly what the recruiter receives.
                </p>
              </div>
            </FadeIn>
            <FadeIn delay={0.4}>
              <div className="col-span-12 md:col-span-8 p-10 border border-border bg-background rounded-mono transition-shadow duration-200 hover:shadow-sm">
                <h3 className="text-2xl mb-4">Architectural Layouts</h3>
                <p className="text-muted leading-relaxed max-w-lg">
                  Moving away from templates toward a modular system.
                  Organize your career narrative through a grid-based approach to information density.
                </p>
              </div>
            </FadeIn>
          </div>
        </div>
      </section>

      {/* Philosophy Section - Split Text/Image */}
      <section className="py-32 px-6">
        <div className="max-w-5xl mx-auto grid grid-cols-12 gap-16 items-center">
          <div className="col-span-12 lg:col-span-6">
            <FadeIn>
              <h2 className="text-4xl md:text-6xl tracking-tighter mb-8">
                Focus on <br />
                <span className="italic">substance.</span>
              </h2>
              <div className="space-y-6 text-lg text-muted leading-relaxed">
                <p>
                  A professional resume should be a reflection of maturity.
                  We reject the trends of soft shadows, rounded pills, and generic gradients.
                </p>
                <p>
                  Our system is based on the principles of Swiss design:
                  a rigid grid, intentional whitespace, and an unwavering focus on content hierarchy.
                </p>
              </div>
            </FadeIn>
          </div>
          <div className="col-span-12 lg:col-span-6">
            <div className="grid grid-cols-2 gap-4">
              {["Grid Precision", "Mono-Chromatic", "High Contrast", "Editorial Feel"].map((text, i) => (
                <FadeIn key={i} delay={0.1 * (i + 1)}>
                  <div className="aspect-square border border-border bg-surface rounded-mono flex items-center justify-center p-6 text-center">
                    <span className="text-xs uppercase tracking-widest text-muted font-medium">{text}</span>
                  </div>
                </FadeIn>
              ))}
            </div>
          </div>
        </div>
      </section>

      <section className="py-32 px-6 border-t border-border">
        <div className="max-w-3xl mx-auto text-center">
          <FadeIn>
            <h2 className="text-4xl md:text-6xl tracking-tighter mb-12">
              Ready to refine <br />
              your professional narrative?
            </h2>
            <Link
              href="/register"
              className="bg-foreground text-background rounded-mono px-10 py-5 transition-transform duration-200 active:scale-95 font-medium text-lg block w-fit mx-auto text-center"
            >
              Build Your Resume
            </Link>
          </FadeIn>
        </div>
      </section>

      <footer className="py-16 px-6 border-t border-border bg-background">
        <div className="max-w-5xl mx-auto flex flex-col md:flex-row justify-between items-center gap-8">
          <div className="font-heading text-2xl tracking-tighter">
            EXECUTIVE<span className="text-muted">MONO</span>
          </div>
          <div className="flex gap-8 text-xs uppercase tracking-widest text-muted font-medium">
            <Link href="#" className="hover:text-foreground transition-colors duration-200">Systems</Link>
            <Link href="#" className="hover:text-foreground transition-colors duration-200">Pricing</Link>
            <Link href="#" className="hover:text-foreground transition-colors duration-200">Terms</Link>
          </div>
          <div className="text-xs text-muted">
            © 2026 Executive Mono.
          </div>
        </div>
      </footer>
    </div>
  )
}
